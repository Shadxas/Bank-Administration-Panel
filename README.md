# Bank-Administration-Panel
A Java-based Bank Administration Panel that lets admins manage users and accounts. It uses OOP with classes like User, Admin, and Account (with types like checking/savings). A central system handles searching, account management, and enforces rules like permissions and eligibility.
